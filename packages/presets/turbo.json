{
	"$schema": "https://turborepo.dev/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"outputs": ["src/**/*.json", "src/**/*.d.ts"]
		},
		"test": {
			"dependsOn": ["^build", "build"],
			"inputs": ["src/**", "test/**", "package.json"],
			"outputs": []
		}
	}
}
